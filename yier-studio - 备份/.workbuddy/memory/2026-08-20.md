# 2026-08-20 工作记录

## 项目名统一到真实项目（参考 E:\yier-studio\index.html）
- 用户指出真实项目名称以 `E:\yier-studio\index.html` 为准（9 个真实项目，均在上海：观唐府·意式风格、金山豪庭·中古奶油、星愉湾·现代意式/轻法式/现代极简、中山文化苑·现代简约、凤凰城、国宸府、国印东方）。
- 已把 portfolio 站三处清单统一为这 9 个：
  - `project-detail.html` 的 `projects` 数据对象（仅留 9 个，含地点；图片用 banner1-6.jpg 占位，说明文案为占位待用户替换）。
  - `projects.html` 的 `filter_gallery` 列表（9 个 li，data-category：6 个私人豪宅 + 3 个私人公寓；办公室/商业空间暂无项目）。
  - `index.html` 最新项目区块（3 个代表：观唐府·意式风格、星愉湾·现代意式、国印东方）。
- 已确认旧占位名（懿贰设计自宅、德钦梅里、成都金融城、汤臣君品、融创外滩、上海壹号院、旭辉府、光明府等无间设计借用名）全部从 HTML 清除。
- main.js 用 decodeURIComponent 解析项目名，「·」和空格可正常作为 URL 参数，无需改脚本。

## 待用户后续处理
- 把 banner1-6.jpg 占位换成各项目自己的图片（替换 `project-detail.html` 的 images 数组与 `projects.html` 的 src）。
- 替换/润色 `project-detail.html` 里每个项目的 design desc 文案。
- 办公室/商业空间分类暂无项目（用户后续自有内容补）。

## 项目精选分类改为纵向排布
- 需求：分类筛选器从横向（inline + `|` 分隔）改为纵向排布，去掉 `|` 分隔，参考 wdesign.hk/?cat=65 的纵向简约风格。
- 改动：
  - `projects.html`：移除 4 个 `<li class="line_ds">|</li>` 分隔符，分类 li 直接纵向。
  - `css/yier-style.css`：`.gallery-filter li` 由 `display:inline` 改为 `display:block`；`a` 改为 `display:block` + 左缩进 `padding:2px 12px` + 左侧 2px 透明竖条（`border-left`），hover 时竖条变灰。
  - `css/main.css`：`.gallery-filter a.active` 由横向下划线 `border-bottom` 改为左侧黑竖条 `border-left-color:#000`，与纵向风格统一。
- 验证：HTML 无 `line_ds` 残留；预览正常；筛选逻辑（main.js 按 data-filter 切换 active）与排布方向无关，仍可用。

## 项目卡片去掉老模板灰色三角装饰（参考 wdesign.hk/?cat=65）
- 用户截图红色箭头标出三处：右上角"Hi/客服"两枚浮窗、项目缩略图下方的灰色三角。
- 先核实前两枚：grep 全 `*.html` 无 `Hi`/`客服`/`hi`/`service`/`kefu` 任何匹配（lang-switch.js 与操作手册的"service"是非 HTML 文本），确认是浏览器扩展/插件浮窗，不是网站代码，**不需要也不应该改网站**。
- 灰色三角来源：`yier-style.css` 的 `.projects-thumb::before`（左半）+ `::after`（右半）伪元素，用 `linear-gradient` 拼成"山/屋顶"形装饰；`.projects-thumb` 的 `padding-bottom: 20px` 为其预留位置。
- 改动：两个伪元素 `display: none`；`.projects-thumb` 的 `padding-bottom` 收为 8px，卡片更紧凑对齐 wdesign 简约风格。
- 验证：预览确认项目下方灰色三角消失，仅剩大图+居中标题，与 wdesign 卡片观感一致。

## 三项框架重构（参考 wdesign.hk 详情页/精选页/社交图标）
- 需求：详情页参考 wdesign.hk/?p=11571-75，精选页参考 wdesign.hk/?cat=65，社交图标参考 wdesign.hk 首页。保留已有文字内容，只改框架/装饰/图标。
- 改动：
  1. **项目详情页 `project-detail.html`**：移除 `#pro_btns` 箭头按钮（CSS 三角箭头+X 关闭按钮）；`#proj` 从左右分栏（左 471px 图 + 右 450px 信息）改为全宽纵向布局（大图在上 max-height 560px + 标题/分割线/信息在下）；底部新增文字导航（上一个/返回/下一个）替代旧箭头；缩略图从 188×126 放大到 220×150。`yier-style.css` 的 `#proj`/`.pro-left`/`.pro-right` 改全宽，`#pro_btns`/`.pro_btn_*` 全 `display:none`。`main.css` 新增 `.project-text`/`.project-divider`/`.project-nav` 样式。`main.js` 的 prev/next 逻辑不变（getElementById 仍可用，href 改文字链接）。
  2. **项目精选页 `projects.html`**：缩略图从 210×150 放大到 240×170；`.projects` 宽度从 730 调到 780；`.recent-project-details` 的 `border-bottom` 从 `#fff` 改 `transparent` + `transition`，消除 hover 时 1px 跳变；hover 去掉 `text-decoration:underline` 更克制。响应式 768px 断点 `.projects` 从 500 调到 540。
  3. **社交图标**：`yier-style.css` 清除 `.icon_xhs`/`.icon_wx` 的 `background-image` 规则（引用不存在的 icon_xhs.png/icon_wx.png），改为 `background:none`；`.sns_icon a` 尺寸统一 24×24（去掉旧 padding-left:20px）。`main.css` SVG override 调为 28px 容器 + 20px 图标 + `fill:#aaa` + hover `#333`，间距 `margin:0 6px`。微信二维码弹窗从 `top:-5px;left:25px;76×75` 改为 `top:30px;居中;100×100`。
- 验证：main.js 语法校验通过；预览详情页大图纵向布局+文字导航正常，精选页卡片放大+分类纵向正常。

## 三项二次调优（参考截图与 wdesign.hk）
- 需求：
  1. 首页显示有问题，参考 wdesign.hk — 轮播图应更沉浸。
  2. 项目精选页 `?cat=65` 上方留白过多，要更紧凑。
  3. 详情页改为截图给出的 wdesign 旧版结构：左侧竖版主图 + 右侧信息 + 下方多列小缩略图（5列x2行）。
- 改动：
  1. **首页**：`index.html` 移除首页最新项目 3 张图的 `.thumb-cover`/`.thumb-more` 装饰，图片尺寸同步 240×170；`main.css` `.home-slider` 高度从 400px 改为 `calc(100vh - 170px)`（min 420 / max 720），`#slider img` 高度 100%；响应式 768px 降至 300px。`yier-style.css` 同步 `#slider {margin-top:0}`、`#slider img {height:100%}`。
  2. **项目精选页**：`#page_title` padding 10→8、margin-bottom 30→15、字号 18→16；`#left_desc` padding-bottom 30→15、加 margin-bottom 15；`#content` margin-top -10→0；`.gallery-filter` margin-bottom 20→10、li line-height 2.4→2.2；`#left ul` 加 margin-top/bottom 0/10，`#left li` line-height 200%→180%。
  3. **项目详情页**：`project-detail.html` 改回左图右信息结构，顶部新增 SVG 箭头导航（prev/X/next），缩略图数据扩到 10 张占位；`yier-style.css` 的 `#pro_btns` 改为居中 SVG 箭头，`.pro-left` 恢复 float left 宽 480px，`.pro-right` float right 宽 460px，`#proj` 恢复 960px 边框；`#imggray` 缩略图改 182×122、间距 3px。`main.css` 更新 `#project_main img` max-height 580px、`.project-divider`、`.detail-nav` SVG 样式、`#imggray` flex 布局 5 列；移动端响应式同步。
- 验证：main.js 语法校验通过；三页预览均正常。

## 红框区域框架对齐（参考截图）
- 需求：用户用红框标注参考站 wdesign.hk 的三个页面红框区域，要求只改框架不改文字。
- 改动：
  1. **projects.html**：左侧 `#left` 结构调整——将 `<ul id="khfw">` 移到 `.sidebar` 前面，改为 `<ul id="jxxm">`（对齐参考站"项目精选"小标题在分类上方的结构）。分类保持纵向无竖线（用户先前指定）。
  2. **about.html**：左侧 `#left_about` 导航从 1 项"关于我们"扩展为 4 项框架（关于我们/公司荣誉/创始人/设计语言），带 `border-left` 竖线分隔，`<ul>` 补 `padding-left:0px`。
  3. **index.html**：红框区域（最新项目文字+3张卡片）结构已在前一轮对齐，本轮未动。
- 验证：本地服务器三页均返回 200，已打开预览。

## 关于我们文字统一小字号（2026-08-20 凌晨）
- 需求：about.html 公司介绍文字统一小字体，不要大小字混杂，不要乱码。
- 改动：
  1. `yier-style.css`：`#middle li` 从 26px/#999 改为 13px/#666/line-height 1.8（实景案例列表原先是大字）。
  2. `about.html`：去掉「18年」「管家式」的 strong 加粗黑色样式；「匠心服务 · 世代传承」从 16px 加粗改为普通 13px 段落；ASCII 直引号 `"..."` 全部换成中文弯引号 `“…”` 防显示异常。
- 验证：文件 UTF-8 编码正常，about.html 返回 200。

## 社交图标中间分隔线对齐（2026-08-20 上午）
- 需求：sns_icon 中间的 `|` 与左右图标垂直对齐（用户截图显示分隔线低于图标）。
- 改动：
  1. `yier-style.css`：`.sns_icon` 加 `line-height: 25px; font-size: 12px;` 与图标同高；新增 `.sns_line` 样式（inline-block / line-height 25 / vertical-align middle / color #ccc / padding 0 2px）。
  2. 7 个 HTML 文件：把 `"><a>` 与 `|<a` 之间的裸文字 `|` 包成 `<span class="sns_line">|</span>`。
- 验证：7 页全部 OK，首页 200。

## 详情页去掉设计公司/地址两行
- 需求：每个项目详情页的「室内设计：懿贰设计」「项目地址：上海·金山」两行去掉。
- 改动：`js/main.js` 的 `projectDetail()` 里 `project_info` 填充只保留「设计说明」一行；`project-detail.html` 数据对象中的 designer/location 字段保留未动（仅不再渲染）。
- 验证：JS 语法校验通过，详情页 200。
