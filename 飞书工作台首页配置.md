# 飞书工作台首页配置指南

---

## 一、首页视图配置

### 方法1：使用"仪表板"视图（推荐）

**步骤：**
```
1. 打开你的飞书多维表格
2. 点击右上角"新建视图"
3. 选择"仪表板"
4. 命名：工作台首页
```

**仪表板配置：**

```
【组件1：统计卡片】
添加4个卡片：
├─ 进行中项目数（数字统计）
│   筛选：状态 = 进行中
│
├─ 本月收入（求和）
│   字段：金额
│   筛选：类型 = 收入，日期 = 本月
│
├─ 待收款（求和）
│   字段：待收款金额
│   筛选：待收款金额 > 0
│
└─ 新案例数（计数）
    筛选：发布状态 = 已发布，发布时间 = 本月

【组件2：项目进度表格】
添加表格组件：
├─ 显示项目表
├─ 筛选：状态 = 进行中
├─ 排序：预计完工日期 正序
├─ 显示字段：项目名称、进度百分比、当前阶段、预计完工日期
└─ 限制行数：显示最近5个

【组件3：今日待办】
这个需要手动维护，飞书不支持自动日历

【组件4：快捷操作】
在首页添加快捷链接：
├─ 新建项目 → 打开项目表
├─ 录入客户 → 打开客户表
├─ 创建报价 → 打开报价表
└─ 录入案例 → 打开案例表
```

---

### 方法2：使用多个表格视图组合

如果仪表板功能不够用，可以用多个视图组合：

**视图1：项目看板**
```
创建看板视图：
├─ 分组：当前阶段
├─ 显示卡片：项目名称、进度、预计完工
└─ 筛选：状态 = 进行中
```

**视图2：客户列表**
```
创建表格视图：
├─ 筛选：客户状态 = 潜在客户
└─ 排序：最后跟进时间 正序
```

**视图3：财务看板**
```
创建表格视图：
├─ 筛选：日期 = 本月
├─ 分组：类型
└─ 汇总：金额求和
```

---

## 二、具体操作步骤

### 创建仪表板视图

```
步骤1：打开项目表
步骤2：点击右上角"新建视图"
步骤3：选择"仪表板"
步骤4：命名"工作台首页"
步骤5：添加组件
```

### 添加统计卡片

```
步骤1：在仪表板点击"添加组件"
步骤2：选择"数字卡片"
步骤3：配置数据源
   - 数据表：选择对应的表
   - 统计类型：计数/求和/平均值
   - 筛选条件
步骤4：设置样式
   - 标题
   - 颜色
步骤5：保存
```

### 添加表格组件

```
步骤1：在仪表板点击"添加组件"
步骤2：选择"表格"
步骤3：配置数据源
   - 选择项目表
   - 设置筛选条件
   - 选择显示字段
步骤4：设置样式
   - 显示行数
   - 是否显示操作列
步骤5：保存
```

---

## 三、各表视图配置建议

### 客户管理表

**视图1：全部客户（表格）**
```
显示字段：客户姓名、电话、项目地址、状态、最后跟进时间
排序：最后跟进时间 倒序
```

**视图2：按状态分组（看板）**
```
分组字段：客户状态
组：潜在客户 | 已签约 | 施工中 | 已交付
```

**视图3：待跟进（表格）**
```
筛选：客户状态 = 潜在客户 或 已签约
筛选：最后跟进时间 < 今天 - 3天
排序：最后跟进时间 正序
```

---

### 项目管理表

**视图1：项目看板（核心）**
```
分组字段：当前阶段
组：设计 | 水电 | 泥瓦 | 木工 | 油漆 | 安装 | 交付

卡片显示：
- 项目名称
- 客户名称
- 进度百分比
- 预计完工日期
```

**视图2：甘特图（时间轴）**
```
开始日期：开工日期
结束日期：预计完工日期
显示字段：项目名称、设计师、进度
```

**视图3：即将到期（表格）**
```
筛选：预计完工日期 <= 今天 + 7天
筛选：状态 = 进行中
排序：预计完工日期 正序
```

---

### 财务记录表

**视图1：本月收支（汇总）**
```
筛选：日期 = 本月
分组：类型
汇总：金额求和
```

**视图2：收入明细（表格）**
```
筛选：类型 = 收入
排序：日期 倒序
```

**视图3：支出明细（表格）**
```
筛选：类型 = 支出
排序：日期 倒序
```

---

### 案例库

**视图1：已发布案例（画册）**
```
筛选：发布状态 = 已发布
显示：案例图片、案例名称、风格
```

**视图2：待发布案例（表格）**
```
筛选：发布状态 = 草稿
排序：创建时间 倒序
```

---

## 四、手机端适配

### 手机端首页

飞书移动端会自动适配，但你可以：

```
1. 将常用的视图添加到"快捷入口"
2. 在首页点击"查看"切换到对应视图
3. 保存为常用视图，方便下次快速进入
```

### 手机端视图优化

```
建议：
- 看板视图：适合手机查看项目进度
- 表格视图：适合手机录入数据
- 画册视图：适合手机查看案例
```

---

## 五、常见问题

### Q1：仪表板组件不显示数据？
```
检查：
1. 数据表是否已添加数据
2. 筛选条件是否正确
3. 关联字段是否正确
```

### Q2：如何固定首页？
```
方法：
1. 创建"工作台首页"视图
2. 点击右上角"..."
3. 选择"固定到首页"
```

### Q3：如何让不同人看到不同首页？
```
方法：
1. 创建多个仪表板视图
2. 设置不同权限
3. 让团队成员各自固定自己需要的视图
```

---

## 六、快速配置清单

### 立即配置（必做）
```
□ 项目表 → 新建看板视图（按阶段分组）
□ 项目表 → 新建甘特图视图
□ 财务表 → 新建本月汇总视图
□ 案例表 → 新建画册视图
```

### 后续优化（可选）
```
□ 客户表 → 待跟进视图
□ 项目表 → 即将到期视图
□ 报价表 → 待确认视图
```

---

## 七、替代方案：多表汇总页面

如果飞书的仪表板功能不够用，可以：

**方案1：创建单独的"工作台"表**
- 手动汇总各项目数据
- 用公式自动计算

**方案2：使用飞书仪表盘**
- 飞书有专门的仪表盘功能
- 可以跨表统计

**方案3：使用第三方工具**
- 如简道云、明道云等
- 提供更强大的仪表板

---

**建议：先用飞书自带的仪表板，不够用再考虑其他方案。**
